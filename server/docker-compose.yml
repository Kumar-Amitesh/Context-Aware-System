services:
  server:
    build: .
    ports:
      - "5000:5000"
    restart: unless-stopped

  redis:
    image: "redis:alpine"
    ports:
      - "6379:6379"
    restart: unless-stopped
    command: ["redis-server", "--bind", "0.0.0.0"]